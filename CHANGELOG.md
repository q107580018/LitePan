# 更新日志

## v0.6.4

### 选中文件悬浮操作栏

- 单选或多选文件/文件夹后，底部自动浮出可点击的操作栏，重命名、移动、复制、删除等操作不再依赖右键菜单，移动端也能直接触达。
- 重命名与下载仅在单选时出现（重命名复用原有行内重命名，下载仅限文件）。
- 移动/复制复用原有目录选择弹窗，单选时使用单删确认文案，多选时走批量确认。
- 操作栏显示已选项目数量与类型，支持一键取消选择，操作执行期间按钮自动禁用。
- 桌面端右键菜单保持不变，与操作栏并存。

## v0.6.3

### PanSou 转存重命名

- 新增可选配置，支持一键转存时使用 PanSou 搜索结果标题作为目标名称。
- 前台和管理端一键转存均支持该配置。
- 将目标名称传递至夸克分享转存接口，关闭配置时保持原始文件名。

## v0.6.2

### PanSou 搜索优化

- 将 PanSou 单次上游请求超时从 10 秒提升至 35 秒，适配冷搜索的多源聚合耗时。
- 调整失败重试策略，减少对 PanSou 冷搜索的重复请求压力。
- 增加 PanSou 搜索失败日志，记录关键词、尝试次数、耗时、HTTP 状态和最终错误。
- 新增异步搜索任务接口，前端提交任务后通过轮询获取结果，避免长时间同步请求被连接超时中断。
- 公共搜索和管理员 PanSou 测试均支持异步任务模式，保留原有同步接口兼容其他调用方。
- 搜索等待超过 8 秒时增加前端提示，说明首次多源聚合可能耗时较长。
- 异步任务当前使用单实例内存存储，服务重启后任务状态会清空。
